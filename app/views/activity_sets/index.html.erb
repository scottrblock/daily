<h1>Listing activity_sets</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @activity_sets.each do |activity_set| %>
  <tr>
    <td><%= link_to 'Show', activity_set %></td>
    <td><%= link_to 'Edit', edit_activity_set_path(activity_set) %></td>
    <td><%= link_to 'Destroy', activity_set, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Activity set', new_activity_set_path %>
